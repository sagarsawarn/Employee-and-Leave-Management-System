/**
 * Development environment.
 * apiBaseUrl points at the CodeIgniter 3 API. The default XAMPP path is
 * awkward (spaces + index.php); replace it with a clean vhost URL later and
 * only this one file changes.
 */
export const environment = {
  production: false,
  apiBaseUrl:
    'http://localhost/Employee%20Management/bcit-ci-CodeIgniter-bcb17eb/index.php/api/v1',
};
